name: afterclass
description: aws infra
runtime:
  name: nodejs
  options:
    packagemanager: pnpm
config:
  pulumi:tags:
    value:
      pulumi:template: aws-typescript
packages:
  planetscale:
    source: terraform-provider
    version: 1.0.2
    parameters:
      - planetscale/planetscale
